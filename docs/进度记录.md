# 律所助手（Lawfirm）进度记录 / 交接

> 给 **wb0（jack）** 和 **ocx（xiangru）** 看：这篇说清楚这个项目是什么、现在做到哪、你们要做什么。
> 更新时间：2026-08-22（周六，Jinhuajie / wb1 在家搭建）

---

## 1. 项目是什么

**企业老板法律助手**——一个手机 H5：老板打字 / 拍照 / 传文件问任何法律问题，大模型识别并解答。
不预设问题类型（合同审查、法规检索、劳动纠纷都算）。

核心架构：手机 H5 → 混元视觉识别图片/文件 → **hy3** 解答（限额/失败自动切 **deepseek**，并标注"备用模型生成"）。
**V0.02 新增**：律师可上传法律条文/法规到「知识库」，模型解答时**优先引用**这些资料。

## 2. 今天（8/22）已完成

- ✅ **两机协同通道打通**：wb1（Windows 192.168.1.9）↔ 家庭 Ubuntu（ocu, `yyh@192.168.1.12`）单向 SSH 互通，消息走 `~/ai_messages` 文件队列。
- ✅ **GitHub 仓库 + deploy key**：`jacksnfeng/lawfirm`，deploy key 两机共用（SSH 别名 `github-lawfirm`），已验证读写。
- ✅ **V0.01 前端 + 真实后端已推上 `main`**：移动端 H5（文字+拍照+文件输入）+ Express 后端，`/api/chat` 已接真实模型（hy3 主力 → deepseek 回退，图片走混元视觉）。
  - key 来自 `D:\Desktop\key.txt`，写入服务端 `.env`（**已 gitignore，不会进仓库**）。
- ✅ **V0.02 参考知识库（wb1 接管完成）**：OCU（yyh）常呆滞（WiFi/有线均抖），按「设计任务优先 ocu、不回复则 wb1 接管」规则，由 **wb1 直接实现并推上 `main`**。
  - 前端新增「知识库」页：上传（多文件）/ 查看列表 / 删除。
  - 后端新增 `POST /api/kb/upload`、`GET /api/kb/list`、`DELETE /api/kb/:name`；`/api/chat` 解答时优先读取 `uploads/kb/` 条文作上下文，并标注引用来源。
  - 本地冒烟通过：上传成功、list 正常、`/api/chat` 实测优先引用知识库内容（例如逾期付款违约金条款）。
- ✅ 仓库自带 `README.md`（怎么跑/怎么部署）和本篇进度记录。

## 3. 待办 / 状态

| 事项 | 状态 / 谁来做 | 说明 |
|------|--------|------|
| 后端接真实 AI | ✅ wb1 已完成 | hy3（主力）→ deepseek（回退），图片走混元视觉 |
| **hy3 主力 key** | ✅ wb0(jack) 实测有效 | `sk-tp-` 系 TokenPlan key，须走专用网关 `api.lkeap.cloud.tencent.com/plan/v3`（旧 tokenhub 地址会 401，已于 2026-08-24 修正） |
| hunyuan / deepseek key | ✅ 已验证有效 | 混元视觉、deepseek 均返回真实内容（已实测） |
| 移动端细节打磨 | wb0(jack) | 输入体验、附件预览、长文本 |
| 部署起服务 | ocx(xiangru) | ⚠️ 线上 zicp.vip 仍是**占位版**（`keysReady` 全 false，缺 `.env`）；需放 `.env`（`HY3_KEY` 等）并重启 `pm2 restart lawfirm` |
| ocu 本地草稿仓库 | 注意 | `~/Lawfirm`（master，无 remote）与 main 分叉，请以 GitHub main 为权威，勿直接推送 |
| 知识库检索增强 | 后续迭代 | 当前为「全文注入」优先引用，可升级为向量检索(RAG) |

## 4. 你们（wb0 / ocx）要做的

- **wb0（jack）**：上 GitHub 看 `jacksnfeng/lawfirm` 的 `README.md` + 本篇，了解全貌；前端（含知识库页）可继续打磨。
- **ocx（xiangru）**：上 GitHub 把仓库 clone 下来，`npm install && npm start` 起服务，配花生壳穿透。
  真实 AI 需要服务端 `.env`（3 个 key）。`.env` **不进仓库**（gitignore），部署时请单独把 key 放到服务器 `lawfirm/.env`（格式见 `.env.example`）。

## 5. 怎么跑（一句话）

```bash
git clone https://github.com/jacksnfeng/lawfirm.git && cd lawfirm && npm install && npm start
```

## 6. 关键端点 / 模型（来自 key.txt，已在 .env 配置）

- 混元多模态视觉：`https://tokenhub.tencentmaas.com/v1`，model `hy-vision-2.0-instruct`
- hy3 推理（TokenPlan）：`https://api.lkeap.cloud.tencent.com/plan/v3`，model `hy3`（2026-08-24 修正：原为 tokenhub 地址导致 401）
- deepseek 备用：`https://api.deepseek.com/v1`，model `deepseek-chat`

## 7. 接口速查（V0.02）

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/chat` | 咨询；支持文字 / 图片 / 文件；优先引用知识库 |
| POST | `/api/kb/upload` | 上传知识库文件（多文件） |
| GET  | `/api/kb/list`  | 已上传知识库文件列表 |
| DELETE | `/api/kb/:name` | 删除指定知识库文件 |
| GET  | `/api/health` | 健康检查（含 keysReady） |

## 8. 仓库整理记录（2026-08-22 11:10，WB1 整理）

今日协同出现「两套草稿并存」的混乱，本次整理已收敛为单一真源：

- ✅ **整合 ocu 文档资产**：ocu 起草的 `docs/律所软件需求说明.md`、`architecture.svg/png`、`HANDOFF.md`、`COLLAB.md`、`DEPLOY-V0.01-OCX.md` 已拉入主仓库 `docs/ocu-draft/`。
- ✅ **修正部署口径（关键）**：ocu 原 DEPLOY 文档的 `.env` 变量名为 `HY3_API_KEY` 系列、启动入口 `server/index.js`，与 main 实际（`HY3_KEY` 系列 + 根 `server.js`）不一致——这正是线上 `keysReady` 全 false 的潜在根因。已在主仓库 `docs/ocu-draft/DEPLOY-V0.01-OCX.md` 中统一修正，请以 `README.md` 第四节 + `.env.example` 为准。
- ✅ **明确权威分支**：GitHub `jacksnfeng/lawfirm` 的 **main** 为权威代码；ocu 本地 `~/Lawfirm`（master，无 remote）仅为早期草稿，**勿基于它直接推送**，以免与 main 冲突。
- ✅ **归档 ocu 悬置任务**：ocu inbox 中过时的 `wb1_task_kb`（V0.02）已归档至 `ai_messages/archive/`；并已发状态消息 `wb1_status_*.json` 告知 ocu 现状。
- ⚠️ **线上仍是占位版**：`http://28i6844q92.zicp.vip/` Express 200 但 `keysReady` 三项全 false——ocx（xiangru）服务器缺 `.env`。请 xiangru 把 `.env`（`HY3_KEY`/`HUNYUAN_KEY`/`DEEPSEEK_KEY`，格式见 `.env.example`）放到 `lawfirm/.env` 并 `pm2 restart lawfirm`。补齐后 hy3 应可用（wb0 实测两把 hy3 key 有效）。

> 现状一句话：代码与文档已统一到 GitHub main；待办只剩「ocx 放 .env 重启」这一件，由 xiangru 处理。

## 10. V0.04 变更（2026-08-24，commit 7e3cd39）

- ✅ **后台 IP 白名单**：所有 `/api/admin/*` 路由加来源 IP 校验，仅放行 `192.168.0.0/24`（办公室局域网，含 jack 机 `192.168.0.131`）+ `127.0.0.0/8`（本机）；公网/外网访问后台一律 403。手机经花生壳公网域名进后台会被拦。
- ✅ **版本号显示**：新增 `GET /api/version` 返回 `V0.04`；`/api/health` 同步返回 version；后台页头显示 V 版本号徽章。
- ✅ **知识库改存 TNAS**：`KB_DIR` 从本地 `uploads/kb` 改为 TNAS 共享挂载点 `~/lawfirm/kb_tnas`（对应 `\\TNAS-YYH\public\数据中心\技术工作\LowFirm法律条文`，SMB IP `192.168.0.103`，凭据 `jack/Jack1930` 存 `/etc/.smbcred_lawfirm`，fstab 持久化）。挂载离线时自动回退本地。
- 现状一句话：V0.04 已上线 main，三个 key 全连通，后台仅局域网可进，知识库落 TNAS。
