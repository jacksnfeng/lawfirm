# 律所助手（Lawfirm）进度记录 / 交接

> 给 **wb0（jack）** 和 **ocx（xiangru）** 看：这篇说清楚这个项目是什么、现在做到哪、你们要做什么。
> 更新时间：2026-08-22（周六，Jinhuajie / wb1 在家搭建）

---

## 1. 项目是什么

**企业老板法律助手**——一个手机 H5：老板打字 / 拍照 / 传文件问任何法律问题，大模型识别并解答。
不预设问题类型（合同审查、法规检索、劳动纠纷都算）。

核心架构：手机 H5 → 混元视觉识别图片/文件 → **hy3** 解答（限额/失败自动切 **deepseek**，并标注"备用模型生成"）。

## 2. 今天（8/22）已完成

- ✅ **两机协同通道打通**：wb1（Windows 192.168.1.9）↔ 家庭 Ubuntu（ocu, `yyh@192.168.1.12`）单向 SSH 互通，消息走 `~/ai_messages` 文件队列。
- ✅ **GitHub 仓库 + deploy key**：`jacksnfeng/lawfirm`，deploy key 两机共用（SSH 别名 `github-lawfirm`），已验证读写。
- ✅ **V0.01 前端骨架已推上 `main`**：移动端 H5（文字+拍照+文件输入）+ 最小 Express 后端（`/api/chat` 占位）。
  - 现在**没填 AI key，所以返回的是占位回复**——这是预期状态，不是 bug。
- ✅ 仓库自带 `README.md`（怎么跑/怎么部署）和本篇进度记录。

## 3. 还差什么（待办）

| 事项 | 谁来做 | 说明 |
|------|--------|------|
| 提供 3 个 key（hy3 / 混元 / deepseek） | Jinhuajie | 写进服务端 `.env`，后端才有真实解答 |
| 后端接真实 AI | wb1 / ocu | 在 `server.js` 的 `/api/chat` 调 hy3 + 混元 + deepseek 回退 |
| 移动端细节打磨 | wb0(jack) | 输入体验、附件预览、长文本 |
| 部署起服务 | ocx(xiangru) | 见 README 第四节，花生壳穿透 |

## 4. 你们（wb0 / ocx）要做的

- **wb0（jack）**：上 GitHub 看 `jacksnfeng/lawfirm` 的 `README.md` + 本篇，了解全貌；前端接下来你接手打磨。
- **ocx（xiangru）**：上 GitHub 把仓库 clone 下来，`npm install && npm start` 起服务，配花生壳穿透。
  起服务前需要 `.env` 里有 3 个 key（目前没 key 也能跑占位版）。

## 5. 怎么跑（一句话）

```bash
git clone https://github.com/jacksnfeng/lawfirm.git && cd lawfirm && npm install && npm start
```
