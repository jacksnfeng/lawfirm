# 律所助手（Lawfirm）进度记录 / 交接

> 给 **wb0（jack）** 和 **ocx（xiangru）** 看：这篇说清楚这个项目是什么、现在做到哪、你们要做什么。
> 更新时间：2026-08-22（周六，Jinhuajie / wb1 在家搭建）

---

## 1. 项目是什么

**企业老板法律助手**——一个手机 H5：老板打字 / 拍照 / 传文件问任何法律问题，大模型识别并解答。
不预设问题类型（合同审查、法规检索、劳动纠纷都算）。

核心架构：手机 H5 → 混元视觉识别图片/文件 → **hy3** 解答（限额/失败自动切 **deepseek**，并标注"备用模型生成"）。

## 2. 今天（8/22）已完成

- ✅ **两机协同通道打通**：wb1（Windows 192.168.1.9）↔ 家庭 Ubuntu（ocu, `yyh@192.168.1.12`）单向 SSH 互通，消息走 `~/ai_messages` 文件队列。
- ✅ **GitHub 仓库 + deploy key**：`jacksnfeng/lawfirm`，deploy key 两机共用（SSH 别名 `github-lawfirm`），已验证读写。
- ✅ **V0.01 前端 + 真实后端已推上 `main`**：移动端 H5（文字+拍照+文件输入）+ Express 后端，`/api/chat` 已接真实模型（hy3 主力 → deepseek 回退，图片走混元视觉）。
  - key 来自 `D:\Desktop\key.txt`，写入服务端 `.env`（**已 gitignore，不会进仓库**）。
- ✅ 仓库自带 `README.md`（怎么跑/怎么部署）和本篇进度记录。

## 3. 还差什么（待办）

| 事项 | 状态 / 谁来做 | 说明 |
|------|--------|------|
| 后端接真实 AI | ✅ wb1 已完成 | `server.js` 已接 hy3（主力）+ 混元视觉（图片）+ deepseek（回退） |
| **hy3 主力 key 失效** | ⚠️ Jinhuajie 待补 | 两把 hy3 key（含标注"废旧链"那把）在腾讯网关均 **401**（Key 不存在/签名失败）。当前文字问答自动回退 deepseek，能正常出真实答案；但 hy3 主力名存实亡，需补一把有效 key |
| hunyuan / deepseek key | ✅ 已验证有效 | 混元视觉、deepseek 调用均返回真实内容（已实测） |
| 移动端细节打磨 | wb0(jack) | 输入体验、附件预览、长文本 |
| 部署起服务 | ocx(xiangru) | 见 README 第四节，花生壳穿透；真实 AI 需服务器侧 `.env` |

## 4. 你们（wb0 / ocx）要做的

- **wb0（jack）**：上 GitHub 看 `jacksnfeng/lawfirm` 的 `README.md` + 本篇，了解全貌；前端接下来你接手打磨。
- **ocx（xiangru）**：上 GitHub 把仓库 clone 下来，`npm install && npm start` 起服务，配花生壳穿透。
  真实 AI 需要服务端 `.env`（3 个 key）。`.env` **不进仓库**（gitignore），部署时请单独把 key 放到服务器 `lawfirm/.env`（格式见 `.env.example`）。当前 hy3 key 失效会回退 deepseek；hunyuan(图片)/deepseek(文字) 可用。

## 5. 怎么跑（一句话）

```bash
git clone https://github.com/jacksnfeng/lawfirm.git && cd lawfirm && npm install && npm start
```

## 6. 关键端点 / 模型（来自 key.txt，已在 .env 配置）

- 混元多模态视觉：`https://tokenhub.tencentmaas.com/v1`，model `hy-vision-2.0-instruct`
- hy3 推理：`https://tokenhub.tencentmaas.com/v1`，model `Hy3`（**key 当前 401 失效**）
- deepseek 备用：`https://api.deepseek.com/v1`，model `deepseek-chat`
